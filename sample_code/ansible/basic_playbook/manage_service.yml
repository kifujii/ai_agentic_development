---
# サービス管理のPlaybook

- name: サービスの管理
  hosts: webservers
  become: yes
  
  tasks:
    - name: サービスを開始
      systemd:
        name: "{{ service_name }}"
        state: started
        enabled: yes
      vars:
        service_name: crond
